# 数字对中仪（Digital Shaft Alignment Tool）使用说明书

## 1. 产品简介
数字对中仪（Digital Shaft Alignment Tool，简称 dsat）用于外业场景下的传感器采样、相机采样、标定执行与数据处理，输出轴系偏移与方位修正结果。

## 2. 运行环境
- Windows 10/11 x64
- .NET Framework 4.6.1 运行环境
- 蓝牙传感器设备（BWT901BLE 系列）
- 网络相机（支持 demo_jpeg_app 配套采集）

## 3. 安装与启动
### 3.1 安装
1. 运行安装包：`dsat_Setup_1.0.0.exe`
2. 在安装向导中选择安装目录
3. 完成安装后可从开始菜单启动 `dsat`

### 3.2 目录结构（安装后）
- `IMU_sample/record`：IMU 采样记录
- `camera_captures/record`：相机采样 CSV
- `camera_captures/log`：相机文本日志
- `camera_captures/preview_stream`：相机预览帧
- `device_info/AT1`：标定输入输出归档

## 4. 主界面说明
### 4.1 左侧功能区
- 连接：传感器连接与相机连接
- 采样记录：IMU采样、相机采样
- 参数设置：IMU 回传速率/带宽、相机采样参数
- 校准与标定：磁场校准、加计校准、Chip Time 校准、相机标定、安装角标定、航向标定
- 数据处理：选择 CSV 并开始处理

### 4.2 右侧显示区
- 上半区：传感器数据
- 下半区：相机日志
- 两个日志框支持右侧纵向滚动轴（内容超出时可下拉查看）
- 顶部告警条支持手动清除与自动消警

## 5. 推荐作业流程
1. 连接传感器（状态灯变绿）
2. 连接相机（状态灯变绿）
3. 执行必要标定（相机/安装角/航向）
4. 开始 IMU采样 与 相机采样
5. 结束采样后在数据处理区选择 CSV
6. 点击开始处理，查看偏差结果与报告

## 6. 标定面板说明
### 6.1 相机标定
- 读取配置参数执行
- 相机未连接时提示先连接后执行

### 6.2 安装角标定
- 自动采集实时 IMU 角度
- 自动读取相机内参和像素参数

### 6.3 航向标定
- 自动采集 IMU 角度
- 支持磁场模式并输出 psi_offset 更新结果

## 7. 告警与日志机制
- 颜色分级：信息/状态/成功/告警/错误
- 告警置顶：关键告警在顶部横幅显示
- 自动消警：30 秒无新告警自动恢复默认状态
- 手动清除：点击“清除”立即恢复默认显示

## 8. 常见问题
### 8.1 提示“未检测到传感器，请先连接后执行”
- 检查传感器连接按钮是否成功
- 检查设备状态灯是否为绿色

### 8.2 提示相机连接失败
- 检查相机 IP 是否正确
- 检查相机与电脑网络连通性

### 8.3 日志过长查看不便
- 使用右侧滚动轴下拉查看历史
- 关键告警可在顶部横幅快速定位

## 9. 输出文件说明
- 标定输入输出 JSON：按时间戳存放于 `device_info/AT1/...`
- 标定摘要 TXT：与输出 JSON 同目录
- 数据处理报告 TXT：与采样 CSV 同目录

## 10. 版本与命名
- 工程/产物命名：`dsat`
- 解决方案：`dsat.sln`
- 主工程：`dsat.csproj`
- 主程序：`dsat.exe`
- 安装包：`dsat_Setup_1.0.0.exe`

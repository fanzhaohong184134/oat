# BWT901BLE 采样日志功能 — 开发计划与设计

## 1. 需求概述

### 1.1 需求一：界面时间戳显示
在主界面数据展示区域，为每台连接的传感器增加 **当前采样时间戳** 的实时显示，精确到毫秒。

### 1.2 需求二：采样日志记录
点击"开始记录"后，按传感器设定的蓝牙采样频率（如 10Hz / 50Hz）进行采样记录：
- 每次采样分配一个 **从 0 开始的递增编号**
- 每条记录必须包含 **时间戳**（精确到毫秒）
- 记录所有九轴数据 + 附加信息
- 点击"停止记录"后，日志保存为 CSV 文件

---

## 2. 采样数据格式设计

### 2.1 CSV 文件格式

文件命名规则：`BWT901BLE_Log_{设备名}_{yyyyMMdd_HHmmss}.csv`

**示例文件名**：`BWT901BLE_Log_WT901_20260708_091530.csv`

### 2.2 表头设计

```csv
SampleNo, Timestamp, ElapsedMs, DeviceMAC, DeviceName, AccX, AccY, AccZ, GyroX, GyroY, GyroZ, AngleX, AngleY, AngleZ, MagX, MagY, MagZ, MagM, Q0, Q1, Q2, Q3, Temperature, PowerPercent, ChipTime, VersionNumber, SerialNumber
```

### 2.3 字段说明

| 序号 | 字段名 | 数据类型 | 单位 | 说明 |
|------|--------|----------|------|------|
| 1 | **SampleNo** | int | - | 采样编号，从 0 开始递增 |
| 2 | **Timestamp** | string | - | 本地时间戳，格式 `yyyy-MM-dd HH:mm:ss.fff` |
| 3 | **ElapsedMs** | double | ms | 距首次采样的相对时间（毫秒），用于分析采样间隔 |
| 4 | **DeviceMAC** | string | - | 设备蓝牙 MAC 地址 |
| 5 | **DeviceName** | string | - | 设备名称 |
| 6 | **AccX** | double | g | X轴加速度 |
| 7 | **AccY** | double | g | Y轴加速度 |
| 8 | **AccZ** | double | g | Z轴加速度 |
| 9 | **GyroX** | double | °/s | X轴角速度 |
| 10 | **GyroY** | double | °/s | Y轴角速度 |
| 11 | **GyroZ** | double | °/s | Z轴角速度 |
| 12 | **AngleX** | double | ° | X轴角度 |
| 13 | **AngleY** | double | ° | Y轴角度 |
| 14 | **AngleZ** | double | ° | Z轴角度 |
| 15 | **MagX** | double | uT | X轴磁场 |
| 16 | **MagY** | double | uT | Y轴磁场 |
| 17 | **MagZ** | double | uT | Z轴磁场 |
| 18 | **MagM** | double | uT | 磁场矢量和 |
| 19 | **Q0** | double | - | 四元数 Q0 |
| 20 | **Q1** | double | - | 四元数 Q1 |
| 21 | **Q2** | double | - | 四元数 Q2 |
| 22 | **Q3** | double | - | 四元数 Q3 |
| 23 | **Temperature** | double | °C | 温度 |
| 24 | **PowerPercent** | double | % | 电量百分比 |
| 25 | **ChipTime** | string | - | 传感器芯片时间 |
| 26 | **VersionNumber** | string | - | 固件版本号 |
| 27 | **SerialNumber** | string | - | 设备序列号 |

### 2.4 数据示例

```csv
# BWT901BLE 采样日志
# 开始时间: 2026-07-08 09:15:30.123
# 设备数量: 1
# 采样频率: 10Hz
# ============================================
SampleNo,Timestamp,ElapsedMs,DeviceMAC,DeviceName,AccX,AccY,AccZ,GyroX,GyroY,GyroZ,AngleX,AngleY,AngleZ,MagX,MagY,MagZ,MagM,Q0,Q1,Q2,Q3,Temperature,PowerPercent,ChipTime,VersionNumber,SerialNumber
0,2026-07-08 09:15:30.123,0.000,AA:BB:CC:DD:EE:FF,WT901(AA:BB:CC:DD:EE:FF),0.012,-0.003,0.998,0.523,-0.112,0.034,1.234,-0.567,178.901,12.34,23.45,45.67,52.12,0.99876,0.01234,-0.00345,0.04567,25.30,75,2026-7-8 9:15:30.001,1.2.3.4,SERIAL001
1,2026-07-08 09:15:30.224,101.000,AA:BB:CC:DD:EE:FF,WT901(AA:BB:CC:DD:EE:FF),0.013,-0.002,0.999,0.545,-0.121,0.029,1.256,-0.543,178.923,12.56,23.67,45.89,52.45,0.99878,0.01256,-0.00321,0.04523,25.31,75,2026-7-8 9:15:30.102,1.2.3.4,SERIAL001
2,2026-07-08 09:15:30.325,202.000,AA:BB:CC:DD:EE:FF,WT901(AA:BB:CC:DD:EE:FF),0.011,-0.004,0.997,0.501,-0.108,0.041,1.212,-0.589,178.879,12.12,23.23,45.45,51.89,0.99874,0.01212,-0.00369,0.04611,25.32,75,2026-7-8 9:15:30.203,1.2.3.4,SERIAL001
```

### 2.5 界面时间戳显示格式

在主界面每台设备数据区域顶部显示：

```
┌─────────────────────────────────────────┐
│ WT901 (AA:BB:CC:DD:EE:FF)              │
│ 采样时间: 2026-07-08 09:15:30.123      │
│ 采样编号: #0    已采样: 0.000s          │
│─────────────────────────────────────────│
│ AccX: 0.012g   AccY: -0.003g  AccZ: 0.998g │
│ GyroX: 0.523°/s  GyroY: -0.112°/s ...      │
│ ...                                         │
└─────────────────────────────────────────┘
```

---

## 3. 界面设计

### 3.1 UI 变更

在 `leftPanel` 的 `groupBox2`（搜索控制组）下方新增 `groupBox4`（日志控制组）：

```
┌─ leftPanel ─────────────────────┐
│ ┌─ groupBox2 (搜索控制) ───────┐ │
│ │  [开始搜索]  [停止搜索]       │ │
│ └──────────────────────────────┘ │
│ ┌─ groupBox4 (日志控制) ───────┐ │
│ │  [开始记录]  [停止记录]       │ │
│ │  状态: 未记录 / 记录中...     │ │
│ │  已记录: 1234 条              │ │
│ └──────────────────────────────┘ │
│ ┌─ groupBox3 (传感器配置) ─────┐ │
│ │  [加计校准] [磁场校准] ...    │ │
│ └──────────────────────────────┘ │
└──────────────────────────────────┘
```

在 `groupBox1` 数据展示区的 `dataRichTextBox` 中，每台设备数据顶部增加时间戳信息。

### 3.2 新增 UI 控件

| 控件名 | 类型 | 说明 |
|--------|------|------|
| `groupBox4` | GroupBox | 日志控制分组框 |
| `startLogButton` | Button | 开始记录按钮 |
| `stopLogButton` | Button | 停止记录按钮（初始禁用） |
| `logStatusLabel` | Label | 日志状态显示 |
| `logCountLabel` | Label | 已记录条数显示 |

---

## 4. 开发计划

### 阶段一：新增日志管理器类（Logger）

**新建文件**: `Sampling/SamplingLogger.cs`

```
功能职责:
├── 管理采样编号（从 0 开始）
├── 记录采样开始时间（用于计算 ElapsedMs）
├── 每次采样时生成采样记录对象
├── 使用 StreamWriter 以追加模式写入 CSV 文件
├── 线程安全的写入机制
└── 提供开始/停止/状态查询接口
```

**核心类设计**:

```csharp
/// <summary>
/// 采样记录数据结构
/// </summary>
public class SamplingRecord
{
    public int SampleNo { get; set; }           // 采样编号
    public DateTime Timestamp { get; set; }     // 绝对时间戳
    public double ElapsedMs { get; set; }       // 相对时间(ms)
    public string DeviceMAC { get; set; }       // 设备MAC
    public string DeviceName { get; set; }      // 设备名称
    public double? AccX { get; set; }
    public double? AccY { get; set; }
    public double? AccZ { get; set; }
    public double? GyroX { get; set; }
    public double? GyroY { get; set; }
    public double? GyroZ { get; set; }
    public double? AngleX { get; set; }
    public double? AngleY { get; set; }
    public double? AngleZ { get; set; }
    public double? MagX { get; set; }
    public double? MagY { get; set; }
    public double? MagZ { get; set; }
    public double? MagM { get; set; }
    public double? Q0 { get; set; }
    public double? Q1 { get; set; }
    public double? Q2 { get; set; }
    public double? Q3 { get; set; }
    public double? Temperature { get; set; }
    public double? PowerPercent { get; set; }
    public string ChipTime { get; set; }
    public string VersionNumber { get; set; }
    public string SerialNumber { get; set; }
}

/// <summary>
/// 采样日志管理器
/// </summary>
public class SamplingLogger
{
    private StreamWriter _writer;
    private int _sampleNo = 0;
    private DateTime _startTime;
    private bool _isRecording = false;
    private readonly object _lock = new object();
    
    public bool IsRecording { get; }
    public int SampleCount { get; }
    
    // 开始记录，创建CSV文件，写入表头
    public void StartRecording(string logDirectory, string deviceInfo);
    
    // 写入一条采样记录
    public void WriteRecord(SamplingRecord record);
    
    // 停止记录，关闭文件
    public void StopRecording();
}
```

### 阶段二：界面时间戳显示

**修改文件**: `Form1.cs` → `GetDeviceData()` 方法

- 在每台设备数据展示的顶部，增加当前时间戳显示
- 格式：`采样时间: yyyy-MM-dd HH:mm:ss.fff`
- 使用 `DateTime.Now.ToString("yyyy-MM-dd HH:mm:ss.fff")` 获取

**修改文件**: `Form1.cs` → `BWT901BLE_OnRecord()` 方法

- 在传感器数据回调时记录当前时间戳

### 阶段三：UI 控件添加

**修改文件**: `Form1.Designer.cs`

- 新增 `groupBox4` 分组框（日志控制）
- 新增 `startLogButton` 按钮
- 新增 `stopLogButton` 按钮
- 新增 `logStatusLabel` 标签
- 新增 `logCountLabel` 标签

**修改文件**: `Form1.resx` / `Form1.zh-CN.resx`

- 添加新控件的本地化文本

### 阶段四：日志记录逻辑集成

**修改文件**: `Form1.cs`

```csharp
// 新增字段
private SamplingLogger _samplingLogger;
private bool _isLogging = false;

// 开始记录按钮事件
private void startLogButton_Click(object sender, EventArgs e)
{
    // 1. 创建 SamplingLogger 实例
    // 2. 生成日志文件路径（程序目录/logs/）
    // 3. 调用 StartRecording()
    // 4. 更新按钮状态
    // 5. 在 BWT901BLE_OnRecord 回调中写入采样数据
}

// 停止记录按钮事件
private void stopLogButton_Click(object sender, EventArgs e)
{
    // 1. 调用 StopRecording()
    // 2. 更新按钮状态
    // 3. 弹出提示保存路径
}

// 修改 BWT901BLE_OnRecord
private void BWT901BLE_OnRecord(Bwt901ble BWT901BLE)
{
    string text = GetDeviceData(BWT901BLE);
    Debug.WriteLine(text);
    
    // 如果正在记录，写入采样日志
    if (_isLogging && _samplingLogger.IsRecording)
    {
        SamplingRecord record = BuildSamplingRecord(BWT901BLE);
        _samplingLogger.WriteRecord(record);
    }
}

// 构建采样记录（从设备数据中提取）
private SamplingRecord BuildSamplingRecord(Bwt901ble device)
{
    return new SamplingRecord
    {
        Timestamp = DateTime.Now,
        DeviceMAC = macAddress,  // 需要从设备获取
        DeviceName = device.GetDeviceName(),
        AccX = device.GetDeviceData(WitSensorKey.AccX),
        // ... 其他字段
    };
}
```

### 阶段五：界面刷新优化

**修改文件**: `Form1.cs` → `RefreshDataTh()` 方法

```csharp
private void RefreshDataTh()
{
    while (EnableRefreshDataTh)
    {
        string DeviceData = "";
        Thread.Sleep(100);
        
        for (int i = 0; i < FoundDeviceDict.Count; i++)
        {
            var keyValue = FoundDeviceDict.ElementAt(i);
            Bwt901ble bWT901BLE = keyValue.Value;
            if (bWT901BLE.IsOpen())
            {
                // ★ 新增：顶部显示时间戳
                DeviceData += "═══════════════════════════════════════\n";
                DeviceData += $"采样时间: {DateTime.Now:yyyy-MM-dd HH:mm:ss.fff}\n";
                if (_isLogging)
                {
                    DeviceData += $"采样编号: #{_samplingLogger.SampleCount}\n";
                }
                DeviceData += "───────────────────────────────────────\n";
                DeviceData += GetDeviceData(bWT901BLE) + "\r\n";
            }
        }
        
        // 更新日志计数显示
        dataRichTextBox.Invoke(new Action(() =>
        {
            dataRichTextBox.Text = DeviceData;
            if (_isLogging)
            {
                logCountLabel.Text = $"已记录: {_samplingLogger.SampleCount} 条";
            }
        }));
    }
}
```

---

## 5. 文件变更清单

| 操作 | 文件路径 | 说明 |
|------|----------|------|
| **新建** | `Sampling/SamplingLogger.cs` | 采样日志管理器类 |
| **新建** | `Sampling/SamplingRecord.cs` | 采样记录数据结构 |
| **修改** | `Form1.cs` | 添加日志记录逻辑、时间戳显示、按钮事件 |
| **修改** | `Form1.Designer.cs` | 添加日志控制UI控件 |
| **修改** | `Form1.resx` | 添加新控件的资源文本 |
| **修改** | `Form1.zh-CN.resx` | 添加新控件的中文资源文本 |
| **修改** | `dsat.csproj` | 注册新文件到项目 |
| **修改** | `dsat_分析文档.md` | 更新功能文档 |

---

## 6. 技术要点

### 6.1 线程安全
- `BWT901BLE_OnRecord` 在后台线程触发，`SamplingLogger.WriteRecord()` 需要加锁
- 文件写入使用 `StreamWriter` + `lock` 保证线程安全

### 6.2 采样时间精度
- 使用 `DateTime.Now` 获取本地时间戳，精度到毫秒
- `ElapsedMs` 使用 `Stopwatch` 或 `DateTime.Now - _startTime` 计算，确保相对时间精确

### 6.3 多设备支持
- 每台设备的采样记录独立编号
- CSV 文件按设备分别生成，或在同一文件中用 DeviceMAC 区分
- 推荐方案：**同一文件，多设备混合记录**，通过 DeviceMAC 和 DeviceName 字段区分

### 6.4 文件存储
- 日志文件保存路径：`{程序运行目录}/logs/`
- 自动创建 `logs` 目录
- 文件编码：UTF-8 with BOM（兼容 Excel 打开）

### 6.5 大文件处理
- 使用 `StreamWriter` 追加写入，避免内存中累积大量数据
- 每条记录实时写入磁盘，防止程序异常退出时数据丢失
- 可选：每 100 条调用一次 `Flush()`，平衡性能和数据安全

---

## 7. 实施顺序

```
步骤1: 创建 Sampling/SamplingRecord.cs（数据结构）
  │
  ▼
步骤2: 创建 Sampling/SamplingLogger.cs（日志管理器）
  │
  ▼
步骤3: 修改 Form1.Designer.cs（添加UI控件）
  │
  ▼
步骤4: 修改 Form1.cs（集成日志逻辑 + 时间戳显示）
  │
  ▼
步骤5: 修改 .csproj（注册新文件）
  │
  ▼
步骤6: 更新 .resx 资源文件（本地化文本）
  │
  ▼
步骤7: 编译测试
  │
  ▼
步骤8: 更新文档
```

---

## 8. 后续扩展（可选）

| 扩展功能 | 说明 |
|----------|------|
| **实时采样频率显示** | 根据实际采样间隔计算并显示实时频率 |
| **采样数据图表** | 使用图表控件实时绘制加速度/角度曲线 |
| **CSV 导入回放** | 支持导入历史 CSV 文件进行数据回放 |
| **日志文件分片** | 当文件超过指定大小（如 100MB）自动创建新文件 |
| **采样时长限制** | 支持设置最大采样时长或最大采样条数 |

# 三个标定的标定设计文档

## 1. 目标

建立三类标定的一致化执行体系：

1. 相机标定（Step 0A）
2. 安装角标定（Step 0B）
3. 航向标定（Step 0C）

要求：

1. 三个按钮分别弹出独立标定面板，风格一致。
2. 每个标定面板完成后由用户确认，确认后关闭。
3. 每个面板都对应一个可执行标定程序（exe）。
4. 航向标定可利用相机标定与安装角标定成果。
5. 在安装目录下维护 `device_info` 设备资料目录，默认设备编号 `AT1`。

---

## 2. 目录与数据协议

### 2.1 根目录

应用安装目录下创建：

`device_info/`

并维护设备编号文件：

`device_info/device_id.txt`（默认内容 `AT1`）

### 2.2 设备目录

按设备编号创建：

`device_info/AT1/`

其下固定三个标定目录：

1. `camera_calibration/`
2. `mounting_calibration/`
3. `heading_calibration/`

每个标定目录下固定子目录：

1. `input/`：本次执行输入配置（json）
2. `output/`：本次执行成果（json + 可选 summary）

---

## 3. 面板风格统一设计

三个面板使用统一 UI 结构：

1. 顶部：步骤标题 + 简述
2. 中部：输入参数区（两列表单）
3. 下部：结果与说明区（只读文本）
4. 底部：状态提示 + 按钮区

按钮区统一：

1. `执行标定`
2. `确认并关闭`（仅成功后可点击）
3. `取消`

交互规则统一：

1. 点击 `执行标定` 前必须完成输入校验。
2. 执行成功后显示结果摘要并启用 `确认并关闭`。
3. 用户点击 `确认并关闭` 后面板关闭。

---

## 4. 三标定流程

## 4.1 相机标定面板（Step 0A）

### 输入项

1. 设备编号
2. `fx, fy, cx, cy`
3. `k1, k2, p1, p2`
4. 图像宽高

### 执行流程

1. 生成 `camera_input_*.json` 到 `camera_calibration/input/`
2. 调用 `CameraCalibration.exe --input ... --output ... --config calibration_config.json`
3. 读取 `camera_output_*.json`
4. 将结果写入 `calibration_config.json`

### 输出成果

1. `camera_calibration/output/camera_output_*.json`
2. 更新后的 `calibration_config.json`

---

## 4.2 安装角标定面板（Step 0B）

### 输入项

1. 设备编号
2. IMU `AngleX`, `AngleY`
3. 靶点像素 `u, v`
4. `fx, fy, cx, cy`

### 执行流程

1. 生成 `mounting_input_*.json` 到 `mounting_calibration/input/`
2. 调用 `MountingCalibration.exe --input ... --output ... --config calibration_config.json`
3. 读取 `mounting_output_*.json`
4. 将 `delta_pitch/delta_roll` 写入 `calibration_config.json`

### 输出成果

1. `mounting_calibration/output/mounting_output_*.json`
2. 更新后的 `calibration_config.json`

---

## 4.3 航向标定面板（Step 0C）

### 输入项

1. 设备编号
2. 是否使用已知方向参考
3. IMU `AngleZ`
4. 磁偏角 `D`
5. 当前 `psi_offset`
6. （已知参考模式）`u1,v1,u2,v2,known_azimuth`

### 依赖信息（只读显示）

1. 相机标定成果：`fx, fy, cx, cy`
2. 安装角标定成果：`delta_pitch, delta_roll`

### 执行流程

1. 生成 `heading_input_*.json` 到 `heading_calibration/input/`
2. 调用 `InstrumentCalibration.exe --input ... --output ... --config calibration_config.json`
3. 读取 `heading_output_*.json`
4. 输出模式说明：
5. 有参考：误差评估与必要时更新 `psi_offset`
6. 无参考：磁场定位模式（精度受损）

### 输出成果

1. `heading_calibration/output/heading_output_*.json`
2. 更新后的 `calibration_config.json`

---

## 5. 输入/输出 JSON 契约

### 相机标定

输入：`CameraCalibrationInput`

1. `DeviceId`
2. `Fx, Fy, Cx, Cy`
3. `K1, K2, P1, P2`
4. `ImageWidth, ImageHeight`

输出：`CameraCalibrationOutput`

1. `Success, Message, ConfigPath`
2. 内参与畸变参数完整回传

### 安装角标定

输入：`MountingCalibrationInput`

1. `DeviceId`
2. `ImuAngleX, ImuAngleY`
3. `UActual, VActual`
4. `Fx, Fy, Cx, Cy`

输出：`MountingCalibrationOutput`

1. `Success, Message, ConfigPath`
2. `DeltaPitch, DeltaRoll`

### 航向标定

输入：`HeadingCalibrationInput`

1. `DeviceId`
2. `HasKnownReference`
3. `ImuAngleZ, MagneticDeclination, CurrentPsiOffset`
4. `U1,V1,U2,V2,KnownAzimuth`
5. `DeltaPitch, DeltaRoll, Fx, Fy, Cx, Cy`（依赖信息）

输出：`HeadingCalibrationOutput`

1. `Success, Mode, Message, ConfigPath`
2. `CurrentPsiOffset, NewPsiOffset`
3. `PredictedAzimuth, Error, Updated`

---

## 6. 异常处理策略

1. exe 不存在：提示缺失路径并阻断确认。
2. 输入参数非法：定位到字段并提示。
3. exe 退出码非 0：显示 stderr/stdout 摘要。
4. 输出文件缺失或反序列化失败：提示“执行异常，未生成有效成果”。
5. `calibration_config.json` 不存在：自动创建后写入。

---

## 7. 与安装打包联动

安装包需要包含：

1. 主程序 `Wit.Example_BWT901BLE.exe`
2. `CameraCalibration.exe`
3. `MountingCalibration.exe`
4. `InstrumentCalibration.exe`
5. 相机依赖与其他运行依赖

首次运行应自动生成：

1. `device_info/device_id.txt`（默认 `AT1`）
2. `device_info/AT1/camera_calibration/{input,output}`
3. `device_info/AT1/mounting_calibration/{input,output}`
4. `device_info/AT1/heading_calibration/{input,output}`

---

## 8. 验证清单

1. 三按钮均可打开各自独立面板。
2. 三面板视觉风格一致。
3. 每次执行均落地 input/output 文件。
4. 用户确认后面板关闭。
5. 航向标定面板可读取并显示相机/安装角标定成果。
6. 安装包运行环境下同样可完成以上流程。

# 免对中校正仪 — 校准与测量流程V2.0

## 流程总览

```
[校准阶段 — 出厂/定期执行]
  Step 0A: 相机内参标定 (棋盘格)
  Step 0B: 相机-IMU安装角偏差标定 (δ_pitch, δ_roll)
  Step 0C: ψ_offset 航向现场校核 / 必要时重标 / 非必须（依靠磁场定位，精度受损）

[测量阶段 — 每次测站执行]
  Step 1: 数据加载与解析
  Step 2: 时间同步与插值
  Step 3: 稳态判定
  Step 4: 图像处理 — 靶标检测
  Step 5: 航向解算
  Step 6: 偏移计算 (像素→ENU，含安装角修正)
  Step 7: 统计输出
```

---

## 校准阶段

### Step 0A: 相机内参标定

| 项目 | 内容 |
|------|------|
| **依赖** | 棋盘格标定板 (如 9×6, 格子边长已知)；相机固定焦距 |
| **输入** | ≥15 张不同角度/距离的棋盘格图像 |
| **输出** | `fx, fy, cx, cy, k1, k2, p1, p2` → 保存为 `camera_intrinsics.json` |

**算法**:

1. 对每张图: `detectChessboardCorners` → `cornerSubPix` 亚像素精化
2. `calibrateCamera(objPoints, imgPoints, imageSize)`
3. 计算重投影误差 RMS < 0.5 px 为合格
4. 输出内参矩阵 K 和畸变向量 D

**执行频率**: 出厂一次；镜头有物理变动时重新标定。

---

### Step 0B: 相机-IMU安装角偏差标定

| 项目 | 内容 |
|------|------|
| **依赖** | Step 0A 完成；设备悬挂静止（稳态）；正下方有可识别靶标 |
| **输入** | 稳态时 IMU 的 pitch₀/roll₀ 读数 + 靶标在图像中的像素坐标 (u, v) |
| **输出** | `δ_pitch`, `δ_roll` (°) → 保存到 `calibration_config.json` |

**算法**:

1. 设备悬挂静止，等待稳态（同 Step 3 条件）
2. 记录 IMU 读数: pitch₀ = AngleY, roll₀ = AngleX
3. 拍照，检测正下方靶标像素坐标 (u_actual, v_actual)
4. 计算安装角偏差:
  δ_pitch = atan((u_actual - cx) / fx) × 180/π - pitch₀
  δ_roll  = atan((v_actual - cy) / fy) × 180/π - roll₀

**验证**: 重复标定 3 次取平均，各次偏差应 < 0.1°。

**对北向判定的影响**: 可忽略（二阶小量 < 0.03°）。

**对偏移计算的影响**: 关键！误差 ≈ H × tan(δ)，必须修正。

**执行频率**: 出厂一次；仅当相机-IMU刚性关系发生变化（如拆装、结构件更换、受撞击后复位）时重新标定。

---

### Step 0C: ψ_offset 航向现场校核 / 必要时重标 / 非必须（依靠磁场定位，精度受损）

| 项目 | 内容 |
|------|------|
| **依赖** | Step 0A、Step 0B 完成；设备安装到三脚架；可选地存在已知方向参考 |
| **输入** | 一次采集的 IMU CSV + Camera CSV；（有参考时）地面两靶标像素坐标与已知真方位角 α_known |
| **输出** | `ψ_offset` 使用值（默认沿用出厂值；必要时更新）→ 保存为 `calibration_config.json` |

**原理**:

Step 0C 不负责标定相机-IMU刚性安装关系；它负责建立本测站的绝对航向基准，或对现有 `ψ_offset` 做现场校核。

若现场有已知精确方向参考，则优先在每次架站后执行 Step 0C，用于绝对航向初始化或快速校核。
若现场没有已知方向参考，则只能退化为磁航向 + 磁偏角修正，Step 0C 为非必须步骤，但相对已知方向参考方案精度会下降。

**算法**:

```
1. 取稳态帧照片，检测 P1(u1,v1) 和 P2(u2,v2)
2. 图像方向角: `θ_img = atan2(-(v2-v1), (u2-u1)) × 180/π`
  - v 向下为正，取负转数学坐标系
3. 同时刻 IMU 真航向: `ψ_true = AngleZ + D` (D 为磁偏角)
4. 若当前没有 `ψ_offset` 初值，则直接计算:
  - `ψ_offset = α_known - ψ_true - θ_img`
5. 若已有 `ψ_offset` 初值，则先预测:
  - `α_pred = ψ_true + ψ_offset + θ_img`
  - `e = α_known - α_pred`
6. 若 `|e| < 1°`，保留当前 `ψ_offset`
7. 若 `|e| >= 1°`，更新:
  - `ψ_offset = ψ_offset + e`
```

**验证**: 旋转设备到另一已知方向，用当前 `ψ_offset` 反算方位角，误差应 < 1°。

**执行频率**: 默认不要求每次重装三脚架都重标；首次进场建议校核，且仅在有方向参考并判定需要时进行重标。无方向参考时可跳过（依靠磁场定位，精度受损）。

---

## 测量阶段

### Step 1: 数据加载与解析

| 项目 | 内容 |
|------|------|
| **依赖** | 两个 CSV 文件存在且格式正确 |
| **输入** | `BWT901BLE_Log_*.csv`, `Camera_Log_*.csv` |
| **输出** | `List<ImuSample>`, `List<CameraFrame>` |

**数据结构**:

```
ImuSample:
  - DateTime Timestamp  (解析 "yyyy/MM/dd HH:mm:ss.fff")
  - double AccX, AccY, AccZ  (单位: g)
  - double GyroX, GyroY, GyroZ  (单位: °/s)
  - double AngleX, AngleY, AngleZ  (单位: °)
  - double MagX, MagY, MagZ  (单位: uT)
  - double Q0, Q1, Q2, Q3

CameraFrame:
  - DateTime Timestamp
  - string FilePath
  - bool Success  (Status == "OK")
```

**注意**: 跳过 CSV 头部 `#` 注释行；只保留 `Status == OK` 的照片。

---

### Step 2: 时间同步与插值

| 项目 | 内容 |
|------|------|
| **依赖** | Step 1 完成；IMU 采样率 ≥ 相机拍照频率 |
| **输入** | `List<ImuSample>`, `List<CameraFrame>` |
| **输出** | `List<SyncedFrame>` — 每帧含照片 + 插值后的 IMU 姿态 |

**算法**:

```
对每个 CameraFrame cf:
  1. 在 ImuSample 列表中二分查找 cf.Timestamp
  2. 找到 t_before ≤ cf.Timestamp ≤ t_after
  3. ratio = (cf.Timestamp - t_before) / (t_after - t_before)
  4. 线性插值:
     AngleX = lerp(imu_before.AngleX, imu_after.AngleX, ratio)
     AngleY, AngleZ, AccX/Y/Z, GyroX/Y/Z 同理
  5. 生成 SyncedFrame { cf, interpolated_imu }
```

**边界处理**: 若照片时间超出 IMU 记录范围，取最近端点值（不外推）。

---

### Step 3: 稳态判定

| 项目 | 内容 |
|------|------|
| **依赖** | Step 2 完成 |
| **输入** | `List<SyncedFrame>` |
| **输出** | `List<SyncedFrame>` (仅稳态帧) |

**算法**:

```
对每个 SyncedFrame sf:
  条件1: |√(AccX² + AccY² + AccZ²) - 1.0| < 0.005  (0.005g 阈值)
  条件2: |GyroX| < 0.3 且 |GyroY| < 0.3 且 |GyroZ| < 0.3  (°/s)

  同时满足 → 标记为稳态候选

连续性检查:
  只有连续 ≥ 3 帧(相机帧)满足条件，才确认为稳态段
  取稳态段内的帧作为有效帧
```

---

### Step 4: 图像处理 — 靶标检测

| 项目 | 内容 |
|------|------|
| **依赖** | Step 3 完成；相机已标定 (Step 0A 的内参) |
| **输入** | 有效帧的图像文件路径、相机内参 `fx, fy, cx, cy, k1, k2, p1, p2` |
| **输出** | 每帧靶标像素坐标 `(u_raw, v_raw)` → 畸变校正后 `(u_corr, v_corr)` |

**算法**:

```
对每张有效帧图像:
  1. 加载图像
  2. 检测靶标中心:
     - 方案A: 圆形靶标 → 霍夫圆检测 / 轮廓拟合椭圆
     - 方案B: 十字标记 → 模板匹配 / 角点检测
     - 方案C: 人工标注 (初期开发阶段)
  3. 得到原始像素 (u_raw, v_raw)
  4. 畸变校正:
     x_norm = (u_raw - cx) / fx
     y_norm = (v_raw - cy) / fy
     r² = x_norm² + y_norm²
     x_corr = x_norm*(1 + k1*r² + k2*r⁴) + 2*p1*x_norm*y_norm + p2*(r²+2*x_norm²)
     y_corr = y_norm*(1 + k1*r² + k2*r⁴) + p1*(r²+2*y_norm²) + 2*p2*x_norm*y_norm
     u_corr = x_corr * fx + cx
     v_corr = y_corr * fy + cy
```

---

### Step 5: 航向解算

| 项目 | 内容 |
|------|------|
| **依赖** | Step 2 完成；`ψ_offset` 已初始化（出厂标定值，或 Step 0C 现场校核后的更新值）；当地磁偏角 D 已知 |
| **输入** | 插值后的 AngleZ（磁北基准）、ψ_offset、磁偏角 D |
| **输出** | 每帧的设备真航向 ψ_true、图像中真北方向角 |

**算法**:

```
ψ_true = AngleZ + D              // 磁北→真北
heading = ψ_true + ψ_offset      // IMU真北→相机X轴参考的真北
图像中真北方向角 = -(heading)     // 从图像X轴正方向逆时针量度
```

**配置参数**:
- `D` (磁偏角): 由测量地点确定，如北京 ≈ -6.5°
- `ψ_offset`: 出厂标定值；必要时由 Step 0C 现场校核更新

---

### Step 6: 偏移计算（含安装角修正）

| 项目 | 内容 |
|------|------|
| **依赖** | Step 4 + Step 5 完成；高度 H 已知；δ_pitch/δ_roll 已标定 (Step 0B) |
| **输入** | `(u_corr, v_corr)`, 内参, 航向, 姿态, H, δ_pitch, δ_roll |
| **输出** | 每帧的 ENU 偏移 `(ΔE, ΔN)` (单位: mm) |

**算法**:

```
// 1. 像素偏移 → 相机坐标系水平位移 (mm)
Δx_cam = (u_corr - cx) / fx * H
Δy_cam = (v_corr - cy) / fy * H

// 2. 倾斜修正（使用 IMU 角度 + 安装角偏差）
true_pitch_rad = (AngleY + δ_pitch) * π / 180
true_roll_rad  = (AngleX + δ_roll)  * π / 180
Δx_cam += H * tan(true_pitch_rad)
Δy_cam += H * tan(true_roll_rad)

// 3. 旋转到 ENU 坐标系
heading_rad = heading * π / 180
ΔE =  Δx_cam * cos(heading_rad) + Δy_cam * sin(heading_rad)
ΔN = -Δx_cam * sin(heading_rad) + Δy_cam * cos(heading_rad)
```

> **注意**: 旋转矩阵正负号取决于相机坐标系定义（X右Y下），安装时需验证。

---

### Step 7: 统计输出

| 项目 | 内容 |
|------|------|
| **依赖** | Step 6 完成，有效帧数 ≥ 5 |
| **输入** | 所有有效帧的 `(ΔE_i, ΔN_i)` |
| **输出** | 最终补偿向量、精度指标、详细报告 |

**算法**:

```
// 1. 异常值剔除 (MAD方法)
median_E = median(ΔE_i)
MAD_E = median(|ΔE_i - median_E|) * 1.4826
保留: |ΔE_i - median_E| < 3 * MAD_E
(ΔN 同理)

// 2. 求均值和标准差
ΔE_final = mean(ΔE_valid)
ΔN_final = mean(ΔN_valid)
σ_E = std(ΔE_valid)
σ_N = std(ΔN_valid)

// 3. 水平综合偏移
ΔH = √(ΔE² + ΔN²)
方位角 = atan2(ΔE, ΔN) * 180/π

// 4. 坐标补偿
E_corrected = E_measured + ΔE_final / 1000  (mm→m)
N_corrected = N_measured + ΔN_final / 1000
```

**输出报告示例**:

```
============ 对中偏移校正报告 ============
有效帧数: 42 / 60
偏移量: ΔE = +1.23 mm, ΔN = -0.87 mm
综合偏移: 1.51 mm, 方位 125.3°
精度(1σ): σE = 0.15 mm, σN = 0.12 mm
=========================================
补偿后坐标:
  E = 500000.000 + 0.001 = 500000.001
  N = 3000000.000 - 0.001 = 2999999.999
=========================================
```

---

## 配置参数汇总

| 参数 | 来源 | 说明 |
|------|------|------|
| `fx, fy, cx, cy` | 相机标定 (Step 0A) | 内参（像素单位） |
| `k1, k2, p1, p2` | 相机标定 (Step 0A) | 畸变系数 |
| `H` (mm) | 现场测量 | 相机到地面垂直距离 |
| `ψ_offset` (°) | 出厂航向标定（默认）/ 现场校核更新 (Step 0C, 必要时) | IMU-相机航向偏差 |
| `δ_pitch` (°) | 安装角标定 (Step 0B) | 相机-IMU pitch 安装偏差 |
| `δ_roll` (°) | 安装角标定 (Step 0B) | 相机-IMU roll 安装偏差 |
| `D` (°) | 查表/计算 | 当地磁偏角 |
| `g_threshold` | 0.005g | 稳态加速度阈值 |
| `ω_threshold` | 0.3°/s | 稳态角速度阈值 |
| `min_stable_frames` | 3 | 最小连续稳态帧数 |

---

## 开发计划

| 阶段 | 任务 | 输出 | 依赖 |
|------|------|------|------|
| **P0** | 基础框架 + 配置文件结构 | 可运行的空壳 | 无 |
| **P0.5** | 相机内参标定工具 (Step 0A) | 标定程序 + JSON输出 | P0 |
| **P0.6** | 相机-IMU安装角标定 (Step 0B) | 标定程序 + δ_pitch/δ_roll | P0.5 |
| **P0.7** | ψ_offset 现场校核/必要时重标工具 (Step 0C，非必须) | 双模式流程 + JSON输出 | P0.5 + P0.6 |
| **P1** | CSV 解析器 (Step 1) | 能正确读取两个CSV | P0 |
| **P2** | 时间同步 (Step 2) | 插值结果验证通过 | P1 |
| **P3** | 稳态筛选 (Step 3) | 合格/不合格帧统计 | P2 |
| **P4** | 靶标检测 (Step 4) | 像素坐标提取 | P3 + P0.5 |
| **P5** | 航向计算 (Step 5) | 图像北向箭头目视确认（支持出厂值或现场更新值） | P2 + P0.7(可选更新) |
| **P6** | 偏移解算 (Step 6) | 已知偏移量闭合检验 | P4 + P5 + P0.6 |
| **P7** | 统计报告 (Step 7) | 最终结果文件 | P6 |
| **P8** | 端到端验证 | 实际精度评估报告 | P7 |

